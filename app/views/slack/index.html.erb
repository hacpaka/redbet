<%= form_tag({ :action => 'update' }) do %>
	<div class="box tabular settings">
		<p>
			<label for="settings_slack_url">URL</label>
			<input type="text" id="settings_slack_url" size=80 value="<%= @url %>" name="settings[slack_url]"/>
		</p>
	</div>

	<%= submit_tag l(:button_save) %>
<% end %>
